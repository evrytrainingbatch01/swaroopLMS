import { HighlightheaderDirective } from './highlightheader.directive';

describe('HighlightheaderDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightheaderDirective();
    expect(directive).toBeTruthy();
  });
});
